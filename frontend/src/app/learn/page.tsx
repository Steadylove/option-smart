'use client';

import { BookOpen } from 'lucide-react';
import { ComingSoon } from '@/components/coming-soon';

export default function LearnPage() {
  return <ComingSoon icon={BookOpen} phase={6} />;
}
