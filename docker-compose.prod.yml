services:
  app:
    image: ghcr.io/steadylove/option-smart:latest
    ports:
      - '${FRONTEND_PORT:-3000}:3000'
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
