target-version = "py311"
line-length = 100

[lint]
select = [
  "E",    # pycodestyle errors
  "W",    # pycodestyle warnings
  "F",    # pyflakes
  "I",    # isort
  "N",    # pep8-naming
  "UP",   # pyupgrade
  "B",    # flake8-bugbear
  "SIM",  # flake8-simplify
  "RUF",  # ruff-specific
]
ignore = [
  "E501",   # line too long (handled by formatter)
  "B008",   # function call in default arg (standard FastAPI Depends pattern)
  "B904",   # raise-without-from in except (intentional for HTTPException — avoids leaking internals)
  "RUF001", # ambiguous unicode — Chinese text uses fullwidth punctuation intentionally
]

[lint.isort]
known-first-party = ["backend"]

[format]
quote-style = "double"
indent-style = "space"
