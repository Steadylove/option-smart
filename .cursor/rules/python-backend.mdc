---
description: "Python backend conventions for FastAPI. Apply when creating or modifying API routes, services, models, or core logic in the backend directory."
globs: "backend/**/*.py"
alwaysApply: false
---

# Python Backend Rules

## Style

- Python 3.11+. Use type hints on all function signatures.
- Format with `ruff format`. Lint with `ruff check`.
- Prefer `async def` for I/O-bound endpoints.
- Use Pydantic models for request/response schemas — never pass raw dicts across boundaries.

## Ruff Lint Rules (pre-commit enforced)

- **F841**: Never assign a variable that isn't used later. Remove or use it.
- **SIM108**: Use ternary `x = a if cond else b` instead of `if/else` blocks for simple assignments.
- **N806**: Variables inside functions must be lowercase. Don't assign class/module aliases with PascalCase inside a function; use `noqa` comment only when absolutely unavoidable.
- Always run `ruff check` mentally before writing code. The pre-commit hook will reject violations.

## Project Structure

```
backend/
  api/          # FastAPI routers
  core/         # Shared logic (greeks calc, risk engine, etc.)
  services/     # External integrations (Longbridge, GLM-5, Telegram)
  models/       # Pydantic schemas & DB models
  tasks/        # Scheduled jobs (alerts, daily reports)
```

## Patterns

```python
# ❌ BAD — raw dict, no types
def calc(data):
    return {"delta": data["iv"] * 0.5}

# ✅ GOOD — typed, clear
def calc_delta(iv: float, spot: float, strike: float) -> float:
    """Compute BS delta from implied volatility."""
    ...
```

- Reuse service singletons via FastAPI dependency injection.
- Keep route handlers thin — delegate logic to `core/` or `services/`.
- Use `logging` module, never `print()`.
